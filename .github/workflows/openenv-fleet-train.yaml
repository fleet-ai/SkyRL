name: "Fleet Task Training (SkyPilot)"

on:
  workflow_dispatch:
    inputs:
      modality:
        description: "Task modality"
        type: choice
        options:
          - tool_use
          - computer_use
        default: tool_use
      env_key:
        description: "Fleet environment filter (empty=all, or: github, booking, reddit, etc.)"
        type: string
        default: ""
      max_tasks:
        description: "Max tasks (empty=all, or number for testing)"
        type: string
        default: ""
      cloud:
        description: "Preferred cloud (any = auto-select cheapest)"
        type: choice
        options:
          - any
          - lambda
          - runpod
          - vast
          - nebius
        default: any
      task_name:
        description: "SkyPilot task name (YAML file in skyrl-train/tasks/)"
        type: string
        default: "openenv-fleet-grpo-qwen3-8b"
      data_version:
        description: "Dataset version in S3 (e.g., v0.1, v0.2, v0.3)"
        type: string
        default: "v0.3"

permissions:
  contents: read

env:
  RUN_NAME_PREFIX: "fleet-task"

jobs:
  launch-training:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      cluster_name: ${{ steps.cluster.outputs.name }}
      wandb_run_name: ${{ steps.cluster.outputs.wandb_run_name }}
      slack_ts: ${{ steps.slack_start.outputs.ts }}
    env:
      WANDB_KEY_TOOL_USE: ${{ secrets.WANDB_API_KEY_TOOL_USE }}
      WANDB_KEY_COMPUTER_USE: ${{ secrets.WANDB_API_KEY_COMPUTER_USE }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install SkyPilot
        run: pip install "skypilot[lambda,runpod,vast,primeintellect,nebius]"

      - name: Configure Cloud Credentials
        run: |
          # Lambda
          mkdir -p ~/.lambda_cloud
          echo "api_key = ${{ secrets.LAMBDA_API_KEY }}" > ~/.lambda_cloud/lambda_keys

          # RunPod
          pip install runpod
          runpod config "${{ secrets.RUNPOD_API_KEY }}"

          # Vast.ai
          mkdir -p ~/.config/vastai
          echo "${{ secrets.VAST_API_KEY }}" > ~/.config/vastai/vast_api_key

          # Prime Intellect
          pip install prime
          prime config set-api-key "${{ secrets.PRIME_INTELLECT_API_KEY }}"

          # Nebius
          mkdir -p ~/.nebius
          echo '${{ secrets.NEBIUS_CREDENTIALS_JSON }}' > ~/.nebius/credentials.json
          echo "${{ secrets.NEBIUS_TENANT_ID }}" > ~/.nebius/NEBIUS_TENANT_ID.txt

      - name: Verify Cloud Credentials
        run: sky check lambda runpod vast primeintellect nebius

      - name: Validate data version
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          DATA_VERSION="${{ inputs.data_version }}"
          S3_BUCKET="fleet-internal-datasets"
          echo "Checking if data version '$DATA_VERSION' exists in S3..."
          if ! aws s3 ls "s3://${S3_BUCKET}/${DATA_VERSION}/openenv/" > /dev/null 2>&1; then
            echo "ERROR: Data version '$DATA_VERSION' not found in S3"
            echo "Available versions:"
            aws s3 ls "s3://${S3_BUCKET}/" | grep PRE | awk '{print $2}'
            exit 1
          fi
          echo "Data version '$DATA_VERSION' validated"

      - name: Generate Cluster Name and Run Name
        id: cluster
        run: |
          TIMESTAMP=$(date -u +"%Y%m%d-%H%M")
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          CLUSTER_NAME="fleet-${{ inputs.modality }}-${SHORT_SHA}-${TIMESTAMP}"
          CLUSTER_NAME=$(echo "$CLUSTER_NAME" | tr '_' '-' | tr '[:upper:]' '[:lower:]')
          echo "name=$CLUSTER_NAME" >> $GITHUB_OUTPUT
          echo "Generated cluster name: $CLUSTER_NAME"

          MODALITY="${{ inputs.modality }}"
          RUN_ID=$(head -c 4 /dev/urandom | xxd -p)
          WANDB_RUN_NAME="fleet_${MODALITY}_${RUN_ID}"
          echo "wandb_run_name=$WANDB_RUN_NAME" >> $GITHUB_OUTPUT
          echo "run_id=$RUN_ID" >> $GITHUB_OUTPUT
          echo "Generated WandB run name: $WANDB_RUN_NAME"

      - name: Launch Training Cluster
        id: launch
        run: |
          MODALITY="${{ inputs.modality }}"
          ENV_KEY="${{ inputs.env_key }}"
          MAX_TASKS="${{ inputs.max_tasks }}"
          CLUSTER_NAME="${{ steps.cluster.outputs.name }}"

          if [ "$MODALITY" == "tool_use" ]; then
            WANDB_KEY="$WANDB_KEY_TOOL_USE"
          else
            WANDB_KEY="$WANDB_KEY_COMPUTER_USE"
          fi

          if [ -z "$WANDB_KEY" ]; then
            echo "ERROR: WANDB_API_KEY_${MODALITY^^} secret is not set!"
            exit 1
          fi

          LAUNCH_CMD="sky launch skyrl-train/tasks/${{ inputs.task_name }}.yaml"
          LAUNCH_CMD="$LAUNCH_CMD --cluster $CLUSTER_NAME"
          LAUNCH_CMD="$LAUNCH_CMD --env WANDB_API_KEY=$WANDB_KEY"
          LAUNCH_CMD="$LAUNCH_CMD --env FLEET_API_KEY=${{ secrets.FLEET_API_KEY }}"
          LAUNCH_CMD="$LAUNCH_CMD --env MODALITY=$MODALITY"
          LAUNCH_CMD="$LAUNCH_CMD --env RUN_ID=${{ steps.cluster.outputs.run_id }}"
          LAUNCH_CMD="$LAUNCH_CMD --env DATA_VERSION=${{ inputs.data_version }}"

          if [ -z "${{ secrets.AWS_ACCESS_KEY_ID }}" ]; then
            echo "ERROR: AWS_ACCESS_KEY_ID secret is required for S3 dataset download"
            exit 1
          fi
          LAUNCH_CMD="$LAUNCH_CMD --env AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}"
          LAUNCH_CMD="$LAUNCH_CMD --env AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}"

          if [ -n "$ENV_KEY" ]; then
            LAUNCH_CMD="$LAUNCH_CMD --env ENV_KEY=$ENV_KEY"
          fi

          if [ -n "$MAX_TASKS" ]; then
            LAUNCH_CMD="$LAUNCH_CMD --env MAX_TASKS=$MAX_TASKS"
          fi

          LAUNCH_CMD="$LAUNCH_CMD -d -y"

          echo "Launching GPU cluster..."
          echo "Command: $LAUNCH_CMD"
          eval "$LAUNCH_CMD" 2>&1 | tee /tmp/launch_output.txt
          LAUNCH_EXIT_CODE=${PIPESTATUS[0]}

          if [ $LAUNCH_EXIT_CODE -ne 0 ]; then
            echo "Cluster launch failed with exit code $LAUNCH_EXIT_CODE"
            cat /tmp/launch_output.txt
            exit $LAUNCH_EXIT_CODE
          fi

          echo "Cluster launched successfully!"
          sky status

      - name: Notify Slack - Training Run Started
        id: slack_start
        if: success()
        uses: slackapi/slack-github-action@v2.0.0
        with:
          method: chat.postMessage
          token: ${{ secrets.SLACK_BOT_TOKEN }}
          payload: |
            {
              "channel": "#fleet-training-runs",
              "text": "🚀 Training Run Started",
              "blocks": [
                {
                  "type": "header",
                  "text": { "type": "plain_text", "text": "🚀 Training Run Started" }
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Run:*\n`${{ steps.cluster.outputs.name }}`" },
                    { "type": "mrkdwn", "text": "*Backend:*\n`SkyRL`" },
                    { "type": "mrkdwn", "text": "*Task:*\n`${{ inputs.task_name }}`" },
                    { "type": "mrkdwn", "text": "*Modality:*\n`${{ inputs.modality }}`" }
                  ]
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Env Filter:*\n`${{ inputs.env_key || 'all' }}`" },
                    { "type": "mrkdwn", "text": "*WandB Run:*\n`${{ steps.cluster.outputs.wandb_run_name }}`" }
                  ]
                },
                {
                  "type": "context",
                  "elements": [
                    { "type": "mrkdwn", "text": "Triggered by: ${{ github.actor }} | <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View Workflow> | <https://wandb.ai/thefleet/fleet-task-grpo|WandB>" }
                  ]
                }
              ]
            }

      - name: Notify Slack - Launch Failed
        if: failure()
        uses: slackapi/slack-github-action@v2.0.0
        with:
          method: chat.postMessage
          token: ${{ secrets.SLACK_BOT_TOKEN }}
          payload: |
            {
              "channel": "#fleet-training-runs",
              "text": "❌ Cluster Launch Failed",
              "blocks": [
                {
                  "type": "header",
                  "text": { "type": "plain_text", "text": "❌ Cluster Launch Failed" }
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Run:*\n`${{ steps.cluster.outputs.name }}`" },
                    { "type": "mrkdwn", "text": "*Task:*\n`${{ inputs.task_name }}`" }
                  ]
                },
                {
                  "type": "section",
                  "text": { "type": "mrkdwn", "text": "*Check logs:*\n<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View Workflow Logs>" }
                }
              ]
            }

  monitor-logs:
    needs: launch-training
    runs-on: ubuntu-latest
    timeout-minutes: 350  # ~5.8 hours, leaving buffer before 6h limit
    outputs:
      training_status: ${{ steps.monitor.outputs.status }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install SkyPilot
        run: pip install "skypilot[lambda,runpod,vast,primeintellect,nebius]"

      - name: Configure Cloud Credentials
        run: |
          mkdir -p ~/.lambda_cloud
          echo "api_key = ${{ secrets.LAMBDA_API_KEY }}" > ~/.lambda_cloud/lambda_keys
          pip install runpod
          runpod config "${{ secrets.RUNPOD_API_KEY }}"
          mkdir -p ~/.config/vastai
          echo "${{ secrets.VAST_API_KEY }}" > ~/.config/vastai/vast_api_key
          pip install prime
          prime config set-api-key "${{ secrets.PRIME_INTELLECT_API_KEY }}"
          mkdir -p ~/.nebius
          echo '${{ secrets.NEBIUS_CREDENTIALS_JSON }}' > ~/.nebius/credentials.json
          echo "${{ secrets.NEBIUS_TENANT_ID }}" > ~/.nebius/NEBIUS_TENANT_ID.txt

      - name: Verify Cloud Credentials
        run: sky check lambda runpod vast primeintellect nebius

      - name: Stream Training Logs
        id: monitor
        run: |
          CLUSTER_NAME="${{ needs.launch-training.outputs.cluster_name }}"
          echo "Streaming logs for cluster: $CLUSTER_NAME"
          echo "This job will run for up to ~5.8 hours."
          echo ""

          # Stream logs - this blocks until job completes or we hit timeout
          sky logs "$CLUSTER_NAME" 2>&1 | tee /tmp/training_logs.txt || true

          # Check final status
          echo ""
          echo "=== Checking cluster status ==="
          sky status "$CLUSTER_NAME" || true

          # Determine outcome
          if grep -q "Job finished (status: SUCCEEDED)" /tmp/training_logs.txt 2>/dev/null; then
            echo "status=SUCCEEDED" >> $GITHUB_OUTPUT
            echo "Training completed successfully!"
          elif grep -q "Job finished (status: FAILED)" /tmp/training_logs.txt 2>/dev/null; then
            echo "status=FAILED" >> $GITHUB_OUTPUT
            echo "Training failed!"
          else
            echo "status=RUNNING" >> $GITHUB_OUTPUT
            echo "Training still running (monitor job timeout or cluster not found)"
          fi

      - name: Cleanup Cluster on Completion
        if: steps.monitor.outputs.status == 'SUCCEEDED' || steps.monitor.outputs.status == 'FAILED'
        run: |
          CLUSTER_NAME="${{ needs.launch-training.outputs.cluster_name }}"
          echo "Training finished. Terminating cluster to save costs..."
          sky down "$CLUSTER_NAME" -y || true

  notify-completion:
    needs: [launch-training, monitor-logs]
    runs-on: ubuntu-latest
    if: always() && needs.launch-training.result == 'success'
    steps:
      - name: Notify Slack - Training Completed
        if: needs.monitor-logs.outputs.training_status == 'SUCCEEDED'
        uses: slackapi/slack-github-action@v2.0.0
        with:
          method: chat.postMessage
          token: ${{ secrets.SLACK_BOT_TOKEN }}
          payload: |
            {
              "channel": "#fleet-training-runs",
              "thread_ts": "${{ needs.launch-training.outputs.slack_ts }}",
              "text": "✅ Training Completed",
              "blocks": [
                {
                  "type": "header",
                  "text": { "type": "plain_text", "text": "✅ Training Completed" }
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Cluster:*\n`${{ needs.launch-training.outputs.cluster_name }}`" },
                    { "type": "mrkdwn", "text": "*Status:*\n`SUCCEEDED`" }
                  ]
                },
                {
                  "type": "section",
                  "text": { "type": "mrkdwn", "text": "*WandB:* <https://wandb.ai/thefleet/fleet-task-grpo|View Results>" }
                }
              ]
            }

      - name: Notify Slack - Training Failed
        if: needs.monitor-logs.outputs.training_status == 'FAILED'
        uses: slackapi/slack-github-action@v2.0.0
        with:
          method: chat.postMessage
          token: ${{ secrets.SLACK_BOT_TOKEN }}
          payload: |
            {
              "channel": "#fleet-training-runs",
              "thread_ts": "${{ needs.launch-training.outputs.slack_ts }}",
              "text": "❌ Training Failed",
              "blocks": [
                {
                  "type": "header",
                  "text": { "type": "plain_text", "text": "❌ Training Failed" }
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Cluster:*\n`${{ needs.launch-training.outputs.cluster_name }}`" },
                    { "type": "mrkdwn", "text": "*Status:*\n`FAILED`" }
                  ]
                },
                {
                  "type": "section",
                  "text": { "type": "mrkdwn", "text": "*Check logs:*\n<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View Workflow Logs>" }
                }
              ]
            }

      - name: Notify Slack - Still Running
        if: needs.monitor-logs.outputs.training_status == 'RUNNING' || needs.monitor-logs.outputs.training_status == ''
        uses: slackapi/slack-github-action@v2.0.0
        with:
          method: chat.postMessage
          token: ${{ secrets.SLACK_BOT_TOKEN }}
          payload: |
            {
              "channel": "#fleet-training-runs",
              "thread_ts": "${{ needs.launch-training.outputs.slack_ts }}",
              "text": "⏳ Training Still Running",
              "blocks": [
                {
                  "type": "header",
                  "text": { "type": "plain_text", "text": "⏳ Training Still Running" }
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Cluster:*\n`${{ needs.launch-training.outputs.cluster_name }}`" },
                    { "type": "mrkdwn", "text": "*Status:*\n`RUNNING`" }
                  ]
                },
                {
                  "type": "section",
                  "text": { "type": "mrkdwn", "text": "GHA log monitoring timed out after ~6 hours. Training continues on GPU cluster.\n\n*Monitor:* <https://wandb.ai/thefleet/fleet-task-grpo|WandB>\n*Logs:* `sky logs ${{ needs.launch-training.outputs.cluster_name }}`\n*Stop:* `sky down ${{ needs.launch-training.outputs.cluster_name }} -y`" }
                }
              ]
            }

      - name: Notify Slack - Monitor Cancelled
        if: needs.monitor-logs.result == 'cancelled'
        uses: slackapi/slack-github-action@v2.0.0
        with:
          method: chat.postMessage
          token: ${{ secrets.SLACK_BOT_TOKEN }}
          payload: |
            {
              "channel": "#fleet-training-runs",
              "thread_ts": "${{ needs.launch-training.outputs.slack_ts }}",
              "text": "⚠️ Workflow Cancelled",
              "blocks": [
                {
                  "type": "header",
                  "text": { "type": "plain_text", "text": "⚠️ Workflow Cancelled" }
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Cluster:*\n`${{ needs.launch-training.outputs.cluster_name }}`" },
                    { "type": "mrkdwn", "text": "*Status:*\n`CANCELLED`" }
                  ]
                },
                {
                  "type": "section",
                  "text": { "type": "mrkdwn", "text": "Workflow was cancelled. Cluster may still be running.\n*Stop:* `sky down ${{ needs.launch-training.outputs.cluster_name }} -y`" }
                }
              ]
            }

      - name: Summary
        if: always()
        run: |
          echo "## SkyRL Training Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Field | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Cluster | \`${{ needs.launch-training.outputs.cluster_name }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Training Status | \`${{ needs.monitor-logs.outputs.training_status || 'UNKNOWN' }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| WandB Run | \`${{ needs.launch-training.outputs.wandb_run_name }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Links" >> $GITHUB_STEP_SUMMARY
          echo "- [WandB Dashboard](https://wandb.ai/thefleet/fleet-task-grpo)" >> $GITHUB_STEP_SUMMARY
